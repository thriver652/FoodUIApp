import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const SweetsComponent = () => {
  return (
    <View>
      <Text style={{color: 'black'}}>SweetsComponent</Text>
    </View>
  );
};

export default SweetsComponent;

const styles = StyleSheet.create({});
